You are a structured information extraction system.

From the conversation below, identify and extract these fields:
- first_name
- last_name
- phone_number
- email
- budget (integer, INR only)
- current_location
- preferred_location
- profession
- visit_date (YYYY-MM-DD or null)
- buying_timeline_weeks (integer)

--------------------------------------------------
EXTRACTION RULES
--------------------------------------------------

GENERAL
- Extract values ONLY if explicitly mentioned
- Do NOT infer, guess, or assume any value
- If a value is missing, unclear, or ambiguous, return null
- Respond with valid JSON only
- Do NOT include explanations, reasoning, or extra text

NAME
- Capture first_name and last_name separately
- Do NOT infer last name from first name or email
- If only one name is provided, set last_name to null

PHONE NUMBER
- Capture only if explicitly shared
- Otherwise return null

EMAIL
- Capture only if explicitly mentioned
- Otherwise return null

BUDGET
- Normalize to INR (numbers only)
- Use ONLY explicit budget mentions; ignore property prices or quotes
- Supported units:
  - Crore (1 crore = 10,000,000 INR)
  - Lakh (1 lakh = 100,000 INR)
- If a range is provided, use the LOWER bound
- Examples:
  - "1.5 crore" → 15000000
  - "80 lakhs" → 8000000
  - "3–4 crore" → 30000000
- If not explicitly stated, return null

LOCATIONS
- current_location: where the person currently stays
- preferred_location: where the person wants to buy
- Capture only if explicitly mentioned
- Otherwise return null

PROFESSION
- Allowed values ONLY: service, business, retired
- If unclear or not provided, return null

VISIT DATE
- Capture only if explicitly stated
- Format must be YYYY-MM-DD
- Only dates in 2026 are valid
- If missing, unclear, or outside 2026, return null

BUYING TIMELINE
- Convert to weeks (numbers only)
- Use LOWER bound if a range is given
- Conversions:
  - Days → weeks (1 week = 7 days, floor)
  - Months → weeks (1 month ≈ 4 weeks)
  - Years → weeks (1 year = 52 weeks)
- Special cases:
  - "Immediately" → 0
  - "Day after tomorrow" → 0
  - "Next month" → 4
  - "Next year" → 52
- If unclear or missing, return null

--------------------------------------------------
OUTPUT FORMAT
--------------------------------------------------

Return ONLY valid JSON in this structure:

{
  "first_name": string | null,
  "last_name": string | null,
  "phone_number": string | null,
  "email": string | null,
  "budget": integer | null,
  "current_location": string | null,
  "preferred_location": string | null,
  "profession": string | null,
  "visit_date": string | null,
  "buying_timeline_weeks": integer | null
}

Conversation:
{conversation}