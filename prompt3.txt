You are a structured information extraction system.

From the conversation below, identify and extract these fields:
- first_name
- last_name
- phone_number
- email
- budget (numeric value only, INR)
- current_location
- preferred_location
- profession
- visit_date (YYYY-MM-DD or null)
- buying_timeline_weeks (numeric value only)

Instructions:
- Use null if a field is not mentioned
- Output must be valid JSON only
- Do not include explanations or extra text

Conversation:
{conversation}
